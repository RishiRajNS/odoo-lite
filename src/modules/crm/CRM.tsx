import React from 'react';
import KanbanBoard from './KanbanBoard';

const CRM: React.FC = () => {
    return (
        <div style={{ height: '100%' }}>
            <KanbanBoard />
        </div>
    );
};

export default CRM;
